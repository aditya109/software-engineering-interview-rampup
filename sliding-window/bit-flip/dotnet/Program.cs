using System;

namespace dotnet
{
    class Program
    {
        static void Main(string[] args)
        {
            // consider all subarrays and find a subarray with maximum value of (count of 1s) - (count of 0s).
            // Let this value be max_diff.
            // Finally return count of zeroes in original array plus max_diff.
            
        }
    }
}
